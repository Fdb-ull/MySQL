# Devuelve todos los campos de la tabla users donde age sea distinto de 15
SELECT * from users WHERE age != 15;
# Devuelve el campo name de la tabla users donde age sea igual a 15
SELECT name FROM users WHERE age = 15;