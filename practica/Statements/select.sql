# Devuelve todos los campos de la tabla users
SELECT * FROM users;
# Devuelve el campo user_id de la tabla users
SELECT user_id FROM users;
# Devuelve los campos user_id y email de la tabla users
SELECT user_id, email FROM users;